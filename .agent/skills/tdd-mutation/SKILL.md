---
name: tdd-mutation
description: ミューテーションテストのためのバグ注入と検証を行う。
---

## 目的 (Goal)
実装コードに意図的なバグ（Mutant）を混入させ、テストがそれを検知できるか（殺せるか）を検証することで、テストの質を担保する。

## ルールと制約 (Rules & Constraints)
1. バグ箇所には必ずマーカー（`// <MUTANT>` 等）を付与すること。
2. 検証後は必ず元の正常なコードに復元すること。
