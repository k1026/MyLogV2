---
name: planning
description: コードの実装・修正前に実行し要件定義や仕様書の更新を行う。
---

# 規則
- **【重要】このスキル呼び出し時はたとえユーザーの指示でもコード実装やファイル・システムの変更を決して行わないこと**。
- `agent/skills/lightSAT-DD/SKILL.md`での実装を意識すること

Step 0. **事前準備**：変更により影響を受ける範囲を絞り込むことで調査のコストを削減する。コマンドの使用時などはコストの低いものを優先する(例：`ls` での構造把握や `grep` でのキーワード検索)。

Step 1. **仕様確認**：ユーザーからの指示を元に`docs/specs/`内の仕様書や既存のコードを分析し矛盾点や不整合、不備などが有れば報告する。

Step 2. **要件定義**：実装・修正の要件を定義し、`docs/specs/*.md`の仕様書の作成または更新する。

Step 3. **実装計画**：要件と既存コードの整合性を確認し、実装計画を立てユーザーに提示する。実装精度を高めるため「1ステップ・1ファイル・1関数」を原則に十分に手順を分割する。テストの精度や作成効率も考慮すること。

Step 4. **記録**：ユーザーに要件定義と実装計画を提示し、`docs/plan/`に`(年月日)-(簡潔な要件名).md`の形式で詳細と手順を出力する。進捗状態は表記しなくて良い。